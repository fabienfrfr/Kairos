Reproducible ablation/exploration logs — not part of the paper draft (see `docs/paper/`).
Each report specifies the exact `kairos`/`torch` versions and CLI commands used, so
results can be regenerated exactly rather than taken on faith.
